#!/usr/bin/env sh

status=$(curl -o /dev/null -s -w "%{http_code}" https://${domain_hf})
echo "$(date "+%Y-%m-%d %H:%M:%S") - Request: https://${domain_hf}, Response: $status" >> /tmp/logs/keepalive.log
