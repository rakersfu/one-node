
# XYCore Release 使用说明

## 项目概述
本项目提供了一个完整的网络工具套件打包压缩包，集成了多个实用的网络服务和工具组件，方便用户快速部署和使用。

## 📦 组件内容
压缩包包含以下核心组件：

- **`xy`**：Xray-core 主程序，高性能网络代理工具
- **`td`**：ttyd 终端工具，提供基于 Web 的终端访问功能
- **`supercronic`**：轻量级定时任务工具
- **`geoip.dat`**：IP 地理位置数据库文件
- **`geosite.dat`**：域名规则数据库文件
- 其他文档文件（LICENSE, README.md）

## 📥 下载与安装

### 1. 下载压缩包
使用 `curl` 命令获取最新的 release 压缩包：

```bash
curl -L -o release-package.tar.gz https://github.com/rakersfu/one-node/releases/download/v1.0.1/release-package.tar.gz
```

### 2. 解压到指定目录
将压缩包解压到 `/opt/xycore/` 目录：

```bash
sudo mkdir -p /opt/xycore
sudo tar -xzf release-package.tar.gz -C /opt/xycore
```

### 3. 创建软链接到系统目录
为了方便在系统中直接调用，可将可执行文件和数据文件软链接到相应目录：

```bash
# 链接可执行文件
sudo ln -sf /opt/xycore/build/xy /usr/local/bin/xy
sudo ln -sf /opt/xycore/build/td /usr/local/bin/td
sudo ln -sf /opt/xycore/build/supercronic /usr/local/bin/supercronic

# 链接数据文件
sudo ln -sf /opt/xycore/build/geoip.dat /usr/local/share/geoip.dat
sudo ln -sf /opt/xycore/build/geosite.dat /usr/local/share/geosite.dat
```

## 📁 目录结构
解压后的目录结构示例如下：

```
/opt/xycore/build/
├── xy                    # Xray-core 主程序
├── td                    # ttyd 终端工具
├── supercronic          # 定时任务工具
├── geoip.dat            # IP 地理位置数据
├── geosite.dat          # 域名规则数据
├── LICENSE               # 许可证文件
└── README.md            # 项目说明文档
```

## ✅ 验证安装
执行以下命令验证各组件是否安装成功：

```bash
xy version
td --version
supercronic --version
```

## 🚀 使用示例

### 启动 Xray-core 服务
```bash
xy -config /path/to/config.json
```

### 启动 ttyd Web 终端服务
```bash
td bash
```

### 使用 supercronic 执行定时任务
```bash
supercronic /path/to/crontab
```

## 📌 注意事项

1. **权限管理**：请确保 `/opt/xycore/` 目录具有适当的执行权限
2. **进程管理**：建议使用 systemd 或 supervisord 来管理各组件进程
3. **数据更新**：数据文件（.dat 文件）建议保持最新版本，以确保规则和地理信息的准确性
4. **安全配置**：根据实际使用场景配置相应的安全策略和访问控制

## 🔧 系统要求
- Linux 操作系统
- 支持的系统架构：x86_64, arm64
- 基本的系统工具：tar, curl

## 📞 技术支持
如遇安装或使用问题，请参考项目文档或联系技术支持团队。
